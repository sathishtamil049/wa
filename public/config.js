/**
 * MilkPro runtime configuration — loaded before the app boots.
 * Edit this file on the server (public_html/config.js) and refresh;
 * no rebuild required.
 *
 * Point the frontend at your cPanel Node.js app, e.g.:
 *   window.__MILKPRO_API__ = "https://api.yourdomain.com";
 *
 * Leave commented out to use the build-time default (http://localhost:3001).
 */
// window.__MILKPRO_API__ = "https://api.yourdomain.com";
